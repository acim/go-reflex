---
kind: pipeline
type: docker
name: default

steps:
- name: docker  
  image: plugins/docker
  settings:
    repo: acim/go-reflex
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password
    tags:
    - 1.13.3
    - latest
    - stable